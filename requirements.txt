# Hand Gesture Recognition — Python dependencies
# Install with: pip install -r requirements.txt
# The hand_landmarker.task model is auto-downloaded on first run.

opencv-python>=4.8.0
# mediapipe 1.0+ removed mp.solutions on Python 3.13/Windows — use 0.10.x
mediapipe>=0.10.30,<1.0.0
numpy>=1.24.0
pycaw>=20230407
comtypes>=1.2.0
